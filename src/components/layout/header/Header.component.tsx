import { Navigation } from "../navigation/Navigation.component"

export const Header = () => {
    return (
        <header className="header" id="home">
            <Navigation />
        </header>
    )
};
